using TestStack.White.UIItems.WindowItems;

namespace TestStackFramework.framework
{
    public class MyWindow
    {
        private Window window;

        public MyWindow(Window window)
        {
            this.window = window;
        }

        
    }
}
